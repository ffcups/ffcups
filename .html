```html
if (datos.logoUrl) {
    document.getElementById('logoContainer').innerHTML =
        `<img src="${datos.logoUrl}" alt="Logo FFCUPS">`;
}
} else {
    document.getElementById('fecha').textContent = datosDefault.fecha;
    document.getElementById('hora').textContent = datosDefault.hora;
}
}

// Mostrar/Ocultar panel admin
function toggleAdmin() {
    const panel = document.getElementById('adminPanel');
    panel.style.display =
        panel.style.display === 'none' ? 'block' : 'none';
}

// Restablecer datos
function resetDatos() {
    localStorage.removeItem('ffcups_datos');
    location.reload();
}

// Formulario de reserva
document.getElementById('formReserva').addEventListener('submit', function(e) {
    e.preventDefault();

    document.getElementById('mensajeExito').classList.add('show');

    // Aquí puedes guardar reservas en el futuro
    const reserva = {
        equipo: document.getElementById('nombreEquipo').value,
        jugador1: document.getElementById('jugador1').value,
        jugador2: document.getElementById('jugador2').value,
        id1: document.getElementById('id1').value,
        id2: document.getElementById('id2').value,
        whatsapp: document.getElementById('whatsapp').value,
        metodoPago: document.getElementById('metodoPago').value
    };

    console.log('Reserva registrada:', reserva);

    this.reset();

    setTimeout(() => {
        document.getElementById('mensajeExito').classList.remove('show');
    }, 5000);
});
</script>
</body>
</html>
```
